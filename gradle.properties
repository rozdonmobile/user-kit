org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.configuration-cache=true
org.gradle.caching=true
kotlin.code.style=official
android.useAndroidX=true
android.nonTransitiveRClass=true

# Maven coordinates, as served by JitPack straight from the GitHub repo:
# `com.github.rozdonmobile:user-kit:<git tag>`. Tag a release with exactly
# VERSION_NAME so the POM and the JitPack version agree. A local checkout can
# still be substituted for the artifact with `includeBuild("../user-kit")`.
GROUP=com.github.rozdonmobile
VERSION_NAME=0.1.0
